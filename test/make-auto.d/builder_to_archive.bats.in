@test 'builder to archive %(tag)s' {
    scope %(scope)s
    %(build_exclude)s
    %(need_docker)s
    
    archive=$(make_archive %(tag)s "$ch_tardir")

#if ( squashfs_ready ); then
 #       archive="${ch_tardir}/%(tag)s.sqfs"
  #  else
   #     archive="${ch_tardir}/%(tag)s.tar.gz"
   # fi
   # make_archive %(tag)s "$ch_tardir"
    archive_grep "$archive"
    archive_ok "$archive"
}
