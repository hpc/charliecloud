Currently there is only one fixture and it's a symlink. This README serves as
a placeholder for future additional fixtures, so Git can manage the directory.

You can see what tests use the fixtures with "misc/grep 'fixtures/'".
